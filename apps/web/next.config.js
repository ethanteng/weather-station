/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Allow Next.js to use the PORT environment variable
  // Next.js will use PORT if set, otherwise defaults to 3000
}

module.exports = nextConfig

